services:
  - type: web
    name: campus-app
    runtime: python
    buildCommand: "echo 'No build needed'"
    startCommand: "python3 run_app.py"
    plan: free
    envVars:
      - key: PORT
        value: 10000
      - key: HOST
        value: 0.0.0.0
